void kill_processes(int *pids, int n) {
    if (n <= 0) return;
    kill(pids[n - 1], SIGKILL);
    kill_processes(pids, n - 1);
}
	_______in parent_____
    } else {
        pids[depth] = pid;
        execute_pipeline(pipes, n, pids, depth + 1);
    }
}